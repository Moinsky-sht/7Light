# 七星灯续命 APP - MVP 完成状态

## ✅ 已完成

### 后端 API（13个端点）
- [x] `POST /api/auth/register` - 用户注册
- [x] `POST /api/auth/login` - 用户登录
- [x] `GET /api/auth/me` - 获取当前用户
- [x] `GET /api/user/home` - 首页数据
- [x] `GET /api/checkin/question` - 获取今日问题
- [x] `POST /api/checkin/answer` - 提交回答（AI解析）
- [x] `GET /api/lamps/status` - 灯阵状态
- [x] `GET /api/social/friends` - 好友列表
- [x] `POST /api/social/add` - 添加好友
- [x] `POST /api/social/boost` - 借火助力
- [x] `GET /api/social/starfield` - 星阵数据
- [x] `GET /api/admin/prompts` - 提示词列表
- [x] `POST /api/admin/prompts` - 创建/更新提示词

### 前端页面（8个）
- [x] `pages/login.vue` - 登录页面
- [x] `pages/register.vue` - 注册页面
- [x] `pages/index.vue` - 首页（点灯打卡）
- [x] `pages/lamps.vue` - 灯阵页面（七星莲灯）
- [x] `pages/social.vue` - 社交页面（灯友圈）
- [x] `pages/profile.vue` - 个人中心
- [x] `pages/admin/prompts.vue` - 管理后台

### 状态管理（Pinia Stores）
- [x] `stores/user.ts` - 用户状态
- [x] `stores/checkin.ts` - 打卡状态
- [x] `stores/lamps.ts` - 灯阵状态

### 组件
- [x] `components/ui/BottomNav.vue` - 底部导航
- [x] `components/svg/CupLamp.vue` - 杯灯 SVG
- [x] `components/svg/LotusLamp.vue` - 莲灯 SVG

### 工具函数
- [x] `composables/useApi.ts` - API 请求封装
- [x] `middleware/auth.ts` - 路由守卫

### 配置
- [x] `.env` - 环境变量
- [x] 数据库 Schema（11张表）

---

## 🚀 启动方式

```bash
cd 1.29/7Light/web
npm install
npm run dev
# 访问 http://localhost:3000 或 http://localhost:3002
```

---

## 📱 使用流程

1. **注册/登录**
   - 访问首页自动跳转登录页
   - 输入手机号和密码注册/登录

2. **每日打卡**
   - 首页显示今日问题
   - 回答3个问题点亮心灯
   - AI 会解析回答并给出反馈

3. **查看灯阵**
   - 七星莲灯展示健康维度
   - 点击灯盏查看详情

4. **灯友互动**
   - 添加好友（邀请码）
   - 借火助力好友

5. **个人中心**
   - 查看统计数据
   - 修改昵称
   - 退出登录

---

## 📁 项目结构

```
1.29/7Light/web/
├── pages/                 # 页面
│   ├── index.vue          # 首页（点灯）
│   ├── login.vue          # 登录
│   ├── register.vue       # 注册
│   ├── lamps.vue          # 灯阵
│   ├── social.vue         # 社交
│   ├── profile.vue        # 个人中心
│   └── admin/prompts.vue  # 管理后台
├── stores/                # 状态管理
│   ├── user.ts
│   ├── checkin.ts
│   └── lamps.ts
├── composables/           # 组合函数
│   └── useApi.ts
├── middleware/            # 中间件
│   └── auth.ts
├── components/            # 组件
│   ├── ui/
│   │   └── BottomNav.vue
│   └── svg/
│       ├── CupLamp.vue
│       └── LotusLamp.vue
├── server/                # 后端 API
│   ├── api/
│   │   ├── auth/
│   │   ├── checkin/
│   │   ├── lamps/
│   │   ├── social/
│   │   ├── user/
│   │   └── admin/
│   ├── db/
│   │   ├── schema.ts
│   │   └── index.ts
│   └── utils/
│       └── jwt.ts
├── .env                   # 环境变量
└── package.json
```

---

## 🎨 设计风格

- **主题**：道家养生 + 中国风
- **配色**：
  - 主色：#A83232（朱砂红）
  - 辅色：#D4AF37（金色）
  - 背景：#F3E9D2（宣纸色）
  - 深色：#2C241B（墨色）
- **字体**：Noto Serif SC（思源宋体）

---

## ⚠️ 注意事项

1. **AI API Key**
   - 通过后台「系统设置」维护 AI 提供商（地址/模型/Key）
   - `.env` 不再承载 AI 的 endpoint/key 配置

2. **数据库**
   - 使用 SQLite，数据存储在 `./data/sevenstar.db`
   - 首次运行会自动创建

3. **端口**
   - 默认 3000，如被占用会自动使用 3002

---

**MVP 已完成，可以开始测试！** 🎉
